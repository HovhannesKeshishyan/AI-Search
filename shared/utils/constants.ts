// Shared between client and server so validation/limits can't drift apart.
export const MAX_IMAGE_SIZE_BYTES = 5 * 1024 * 1024;
export const PRODUCTS_PAGE_SIZE = 10;
export const PRODUCTS_MAX_PAGE_SIZE = 50;
